describe('ComponentName.cy.ts', () => {
  it('Shoud return true title component was correct', () => {   //VERIFICAR SE O MEU TITULO ESTÁ CORRETO
    // cy.mount()
    cy.visit("/") //PASSO A BARRA VAZIA PORQUE JÁ ESTA DEFINIDO NO FICHEIRO cypress.config.ts o root inicial

  })
})
